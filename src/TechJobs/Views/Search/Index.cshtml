<h1>@ViewBag.title</h1>

<form action="/Search/Results">
    <h2>Search by:</h2>

    <p>
        @foreach (KeyValuePair<string, string> column in ViewBag.columns)
        {
            <span>
                <input type="radio"
                       name="searchType"
                       id="@column.Key"
                       value="@column.Key"
                       checked="@column.Key == 'all''" />
                <label for="@column.Key">@column.Value</label>
            </span>
        }
    </p>

    <p>
        <label for="searchTerm">Keyword:</label>
        <input type="text" name="searchTerm" id="searchTerm" />
    </p>

    <input type="submit" value="Search" />
</form>

<hr />

@if (ViewBag.jobs != null)
{

    <h3>@ViewBag.jobs.Count Result(s)</h3>

    // TODO #2 - Loop over jobs dictionary and display all job fields
    //foreach dictionary in jobs, foreach KeyValuePair in dictionary

    // Table should be in dictionary loop, with cells being in KeyValuePair loop.
    @foreach (Dictionary<string, string> jobs in ViewBag.jobs)
    {
        @foreach (KeyValuePair<string, string> job in jobs)
        {
            <table class="job-listing">
                <tr>
                    <td>@job.Key @job.Value</td>
                </tr>
            </table>
            <br />
            <br />
        }
    }
}